// package com.vladkopanev.zio.saga

// import org.scalatest.FlatSpec
// import org.scalatest.Matchers._

// // import com.dimafeng.testcontainers.PostgreSQLContainer
// // class PostgresqlSpec extends FlatSpec with ForAllTestContainer  {

// //   override val container = PostgreSQLContainer()

// //   "PostgreSQL container" should "be started" in {
// //     Class.forName(container.driverClassName)
// //     val connection = DriverManager.getConnection(container.jdbcUrl, container.username, container.password)
// //   }
// // }

// import com.testcontainers.container.PostgreSQLContainer //  {ForAllTestContainer, MySQLContainer}

// class MysqlSpec extends FlatSpec {

//   override val container = PostgreSQLContainer()

//   // it should "do something" in {
//   //   Class.forName(container.driverClassName)
//   //   val connection = DriverManager.getConnection(container.jdbcUrl, container.username, container.password)
//   // }
// }
